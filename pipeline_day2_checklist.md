# Day 2 Pipeline Roadmap
*Status: Complete*

## Logical Reasoning & Context Patches
- [x] **Task 1 (Blueprint Gate):** In `mesh_loops.py` (Phase 0.5), right after generating and saving `project_blueprint.md` but *before* extracting the first task and falling through to Phase 3, added a terminal `input()` asking the user: "Do you approve this architectural blueprint? [Y/n]". If 'n', pause and tell the user to edit the markdown file manually, then press Enter to continue.
- [x] **Task 2 (Sibling Rolling Context):** In `mesh_loops.py` (Phase 4), sibling tasks are currently blind to each other. Before calling `execute_task`, aggregate the outputs of all previously completed sibling tasks (tasks with the same parent, or top-level tasks from the same run) from `ctx.all_results_dict`. Pass this as a new `sibling_context` variable to `execute_task` (update `_pipeline_helpers.py` to accept and inject this into the prompt). (mesh_loops.py lines ~339-371, _pipeline_helpers.py lines ~236-239)
- [x] **Task 3 (Director Context):** In `mesh_loops.py` (Phase 3), the Director is decomposing tasks blindly. Inject `ctx.gdd_context` and `ctx.project_state` directly into the `director_input` prompt string before calling `call_ollama`. (mesh_loops.py lines ~209-218)
- [x] **Task 4 (Architect Identity Crisis):** In `mesh_finalize.py` (Phase 6), a generic `ARCHITECT_FIX_SYSTEM` is used to fix Reviewer fails. Deprecate this. Refactor the fix loop to iterate over the failing tasks and route the Reviewer's critique back to the *original domain agent* (using `get_agent_system(domain)`), so the agent fixing the code retains its strict C++/Lua domain rules. (mesh_finalize.py lines ~127-192)
- [x] **Task 5 (SOS Formatting Mandate):** In `_pipeline_helpers.py` (`generate_failure_report`), the `External Agent SOS Prompt` now includes strict formatting rules: `### [Feature_Name]` ledger headers, engine constraints (SDL2/Jolt/Box2D/Lua/sol2), verbose runtime log formatting, and task recovery approach. (_pipeline_helpers.py lines ~430-470)
